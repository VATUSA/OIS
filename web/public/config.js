// Runtime API base is injected here in production container images (from OIS_API_URL, via
// deploy/40-ois-config.sh). Left empty in local dev so the build-time VITE_OIS_API_URL /
// localhost default is used instead.
