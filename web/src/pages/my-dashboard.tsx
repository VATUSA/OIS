import {Dashboard} from "@/features/dashboard/Dashboard";

/** The customizable personal dashboard — a grid of user-arranged widgets (see features/dashboard). */
export function MyDashboardPage() {
  return <Dashboard />;
}
