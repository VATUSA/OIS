{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions for the OIS main window. Intentionally just Tauri's core defaults — desktop features add their own capability entries as they land (#345-#347).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
