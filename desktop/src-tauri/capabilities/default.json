{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the OIS main window and the windows it opens: Tauri's core defaults, signed auto-update (#347), native notifications plus the window controls a notification click needs (#348), creating / placing / pinning pop-out panels (#349) and whole-route windows (#350), the menu-bar tray (#351), and OS-level global shortcuts (#352). Tray and menu themselves are already covered by core:default.",
  "windows": [
    "main",
    "popout-*",
    "window-*"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart",
    "notification:default",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "core:window:allow-show",
    "core:webview:allow-create-webview-window",
    "core:window:allow-set-always-on-top",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-position",
    "core:window:allow-set-size",
    "core:window:allow-outer-position",
    "core:window:allow-outer-size",
    "core:app:allow-default-window-icon",
    "core:window:allow-hide",
    "process:allow-exit",
    "autostart:default",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-unregister-all",
    "global-shortcut:allow-is-registered"
  ]
}
