[package]
name = "ois-client"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
# Typed Rust client for the OIS backend REST API. Used by the Discord bot to act as
# a service account (bearer token). Kept dependency-light and DB-free.
[dependencies]
reqwest.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
