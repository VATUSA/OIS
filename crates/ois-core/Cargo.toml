[package]
name = "ois-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
# Pure domain + permission types. NO database or web-framework deps, so both the
# backend and the Discord bot can depend on it. DB-backed resolution lives in the backend.
[dependencies]
serde.workspace = true
serde_json.workspace = true
utoipa.workspace = true
